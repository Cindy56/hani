<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.game.trade.modules.member.dao.MemberPlayConfigDao">
    
	<sql id="memberPlayConfigColumns">
		a.id AS "id",
		a.user_id AS "user.id",
		a.user_name AS "userName",
		a.account_id AS "accountId",
		a.play_config AS "playConfig",
		a.create_date AS "createDate",
		a.create_by AS "createBy.id",
		a.update_date AS "updateDate",
		a.update_by AS "updateBy.id",
		a.del_flag AS "delFlag",
		u2.name AS "user.name"
	</sql>
	
	<sql id="memberPlayConfigJoins">
		LEFT JOIN sys_user u2 ON u2.id = a.user_id
	</sql>
    
	<select id="get" resultType="MemberPlayConfig">
		SELECT 
			<include refid="memberPlayConfigColumns"/>
		FROM member_play_config a
		<include refid="memberPlayConfigJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="MemberPlayConfig">
		SELECT 
			<include refid="memberPlayConfigColumns"/>
		FROM member_play_config a
		<include refid="memberPlayConfigJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="MemberPlayConfig">
		SELECT 
			<include refid="memberPlayConfigColumns"/>
		FROM member_play_config a
		<include refid="memberPlayConfigJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO member_play_config(
			id,
			user_id,
			user_name,
			account_id,
			play_config,
			create_date,
			create_by,
			update_date,
			update_by,
			del_flag
		) VALUES (
			#{id},
			#{user.id},
			#{userName},
			#{accountId},
			#{playConfig},
			#{createDate},
			#{createBy.id},
			#{updateDate},
			#{updateBy.id},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE member_play_config SET 	
			user_id = #{user.id},
			user_name = #{userName},
			account_id = #{accountId},
			play_config = #{playConfig},
			update_date = #{updateDate},
			update_by = #{updateBy.id}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE member_play_config SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
	<select id="getMemberPlayConfigByUserId" resultType="MemberPlayConfig">
		SELECT 
			<include refid="memberPlayConfigColumns"/>
		FROM member_play_config a
		<include refid="memberPlayConfigJoins"/>
		WHERE a.user_id = #{userId}
	</select>
	
	
	<select id="queryByAccountIdList" resultType="MemberPlayConfig">
		SELECT 
			a.id AS "id",
			a.user_id AS "user.id",
			a.user_name AS "userName",
			a.account_id AS "accountId",
			a.play_config AS "playConfig",
			a.create_date AS "createDate",
			a.create_by AS "createBy.id",
			a.update_date AS "updateDate",
			a.update_by AS "updateBy.id",
			a.del_flag AS "delFlag"
		FROM member_play_config a
		where a.account_id in
		 <foreach item="item" collection="accountIds" open="(" separator="," close=")">
            #{item}
        </foreach>
	</select>
	
</mapper>